rules:
  - cre:
      id: regex-example-2
    metadata:
      id: rule-id
    rule:
      set:
        event:
          source: cre.log.kafka
        match:
          - regex: "\\[emerg] (.+) still could not bind()"
