rules:
  - cre:
      id: bad-seq-one-negate-condition
    metadata:
      id: rule-id
    rule:
      sequence:
        event:
          source: kafka
        negate:
          - regex: "foo(.+)bar" # one negate condition not allowed
