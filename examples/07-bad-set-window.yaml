rules:
  - cre:
      id: bad-window
    metadata:
      id: rule-id
    rule:
      set:
        # missing window
        event:
          source: cre.log.kafka
        match:
          - foo
          - bar
