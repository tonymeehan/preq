rules:
  - cre:
      id: bad-window
    metadata:
      id: rule-id
    rule:
      set:
        # missing window
        event:
          source: kafka
        match:
          - foo
          - bar
