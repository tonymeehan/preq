rules:
  - cre:
      id: bad-regex-example
    metadata:
      id: rule-id
    rule:
      set:
        event:
          source: cre.log.kafka
        match:
          - "\\[emerg] (.+) still could not bind()" # use regex: instead
